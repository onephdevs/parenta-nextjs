'use client';

/**
 * @deprecated Import from `@/components/ui` (or `@/components/ui/FormField`).
 * Re-export kept for compatibility.
 */
export { FormField, type FormFieldProps } from '@/components/ui/FormField';

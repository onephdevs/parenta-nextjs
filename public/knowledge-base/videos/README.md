Walkthrough `.webm` files for the office Knowledge base.

Successful training recordings copy here automatically (same file names as `recordings/`). To copy by hand:

```bash
cp recordings/*.webm public/knowledge-base/videos/
```

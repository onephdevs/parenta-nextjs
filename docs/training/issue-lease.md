---
# Job: Issue or store a lease document

**Replaces:** Printed contract in a folder, or a signed PDF in Messenger / Drive.

**Who does this:** Office admin

**When:** After a tenant is assigned to a unit, or when a signed copy comes back.

## Steps
1. Sidebar **Tenants** → open the tenant → **Documents** (`/admin/tenants/[id]?tab=documents`).
2. Under **Upload / e-sign lease**, click **Generate lease** (draft from assignment and building policies) or **Upload** / **Upload signed PDF** (PDF, DOC, or DOCX, max 10MB).
3. If e-sign is needed, type the name, accept the terms, and click **Sign as landlord** (or **Sign lease**). Use **Regenerate lease** only if you must replace the file.

## Also on this page
**Documents tab (this job)**
- **Required Documents:** **Lease Contract**, **ID Requirements**, **Proof of Residency**.
- Empty lease: **No lease agreement on file** / **Generate a draft from lease data, or upload a signed PDF.**
- After a file: **Lease agreement on file**; **View**, **Delete**.
- **Sign as landlord**, **Sign as witness**; **Type your full legal name**; checkbox agreeing to terms; **Sign lease**; **View lease**.
- Status: **Tenant signed** / **Awaiting tenant**; **Landlord signed** / **Awaiting landlord**.
- **Optional Documents** / **Add Document** — table **Full Name**, **ID Type**, **File**, **Actions**. Pagination 5 per page. Empty: **No optional documents yet.**
- No search / filter / sort / export / print on this tab.
- Delete confirm: **Delete document?**
- Replace confirm if already signed: **Replace signed lease?** → **Replace & generate**.
- Upload errors: **Invalid File**, **File Too Large** (10MB).

**Same tenant, other tabs (not this job)**
- Profile, Lease, Financials — see **Add Tenant**.

**Sidebar Documents** (`/admin/documents`) — separate library, not this tab
- **Upload document**, **Manage Categories**.
- Search **Filename, tenant...**; **Category**, **Building**, **Status** (**Signed**, **On file**, **Expiring soon**, **Needs review**).
- Row: **View**, **Download**, **Edit**, **Delete**.
- Bulk (when selected): **Categorize**, **Download ZIP**, **Delete**.
- See **File a contract or ID in Documents**.

**How the PDF looks**
- **Documents** → **Lease Designer** — edit clauses, **Print**, **Export PDF**.
- **Leasing** → **Lease Templates** — deposit / term / penalty package used when assigning a room.

## Done when
**Lease Contract** under **Required Documents** shows the file after refresh. The tenant can open it under portal **Documents**.

## Watch out for
**Generate lease** needs an active room assignment. If a signed lease is already on file, the app asks to confirm replace before overwriting. Sign needs a typed name and the agreement checkbox.

## Video
[link placeholder]
---
